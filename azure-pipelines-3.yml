trigger:
- master

pool:
  name: 'private_pool'

variables:
  SUDO_PASSWORD: Baba12224454

steps:
- checkout: self

- script: |
    echo "${SUDO_PASSWORD}" | sudo -S apt-get update
    echo "${SUDO_PASSWORD}" | sudo -S apt-get install -y docker.io
    echo "${SUDO_PASSWORD}" | sudo -S systemctl start docker
    echo "${SUDO_PASSWORD}" | sudo -S systemctl enable docker
    echo "${SUDO_PASSWORD}" | sudo -S usermod -aG docker $(whoami)
  displayName: 'Install Docker'

# Build Docker Image
- script: |
    docker build -t my-python-image .
  displayName: 'Build Docker Image'

# Run Tests in Docker Container
- script: |
    docker run --rm -v $(Build.SourcesDirectory):/app -v $(Build.ArtifactStagingDirectory):/artifacts my-python-image
  displayName: 'Run Tests in Docker Container'

# Publish Allure Report
- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.SourcesDirectory)/allure-report'
    ArtifactName: 'AllureReport'
  displayName: 'Publish Allure Report'

# Publish JUnit Report
- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.SourcesDirectory)/junit-results.xml'
    ArtifactName: 'JUnitReport'
  displayName: 'Publish JUnit Report'